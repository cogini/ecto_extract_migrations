defmodule <%= repo %>.Migrations.<%= module_name %> do
  use Ecto.Migration

  def up do
    execute(
    """
    <%= Regex.replace(~r/^/m, sql, "    ") %>
    """)
  end

  def down do
    execute("drop extension if exists <%= name %>")
  end
end
